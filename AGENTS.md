# Repository Agent Notes

- Keep `/web` as the source of truth and never commit `/dist`.
- Prefer small ES module files over monolithic scripts.
- Use relative paths for all browser-loaded assets.
- Keep shell scripts POSIX-friendly where practical.
